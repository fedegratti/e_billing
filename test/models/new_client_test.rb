require 'rspec'

describe 'New client' do

  it 'without data' do
    expect(false).to eq(true)
  end

  it 'with data' do
    expect(false).to eq(true)
  end

  it 'with invalid data' do
    expect(false).to eq(true)
  end
end